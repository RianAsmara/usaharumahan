upload_max_filesize = 10M
post_max_size = 12M
memory_limit = 256M
max_execution_time = 60
date.timezone = Asia/Jakarta

; PII fields (customer address, phone) can appear in uncaught exception traces.
; Trim what error logs retain instead of disabling display_errors app-wide.
display_errors = Off
log_errors = On
